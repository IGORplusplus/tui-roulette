//this is essentially a config file and is what all of the widgets are based off of
use ratatui::style::Color;

struct Colors {
    background: Color,
    primary: Color,
    secondary: Color,
    active: Color,
}
